# NetAtlas 运行依赖（宽松约束；精确锁定见 requirements.lock）
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
PyYAML>=6.0,<7.0
zstandard>=0.22,<1.0
pyarrow>=15.0
duckdb>=0.10
psutil>=5.9
geoip2>=4.8 ; platform_system != "Darwin"
scapy>=2.6.0
# 测试依赖（pytest 可选，unittest 亦可运行）
pytest>=8.0 ; extra == "test"
httpx>=0.27 ; extra == "test"
